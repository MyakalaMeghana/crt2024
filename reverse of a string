#include <stdio.h>

int main() {
    char str[50],str1[50];
    scanf("%s",str);
   int i,j=0,n;
    for(n=0;str[n]!='\0';n++);
    for(i=n-1;i>=0;i--)
    {
        str1[j]=str[i];
        j++;
    }
    str[j]='\0';
    printf("%s",str1);
    return 0;
}
