#include<stdio.h>
#include<string.h>
int main()
{
char str1[50],str2[50];
    char str3[100];
    scanf("%s",str1);
    scanf("%s",str2);
    int i,j;
    for(i=0;str1[i]!='\0';i++)
    {
        str3[i]=str1[i];
    }
    for(j=0;str2[j]!='\0';j++)
    {
        str3[i+j]=str2[j];
    }
   i=strcmp(str1,str2);
   if(i==0)
    {
        printf("equal");
    }
    else
    printf("not equal");
return 0;
}
